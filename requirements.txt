python-fasthtml>=0.1.0
django>=4.2.0
django-machina>=1.2.0  # Forum framework
django-haystack>=3.2.1  # Required by django-machina for search
django-widget-tweaks>=1.5.0  # Required by django-machina for form rendering
Whoosh>=2.7.4  # Search backend for django-haystack
Pillow>=10.0.0  # Required for image handling
python-dotenv>=1.0.0
httpx>=0.24.0
starlette>=0.27.0
uvicorn>=0.23.0
python-multipart>=0.0.6
itsdangerous>=2.1.2  # for session handling
django-allauth>=0.57.0  # for OAuth (GitHub, GitLab)

# Critical dependencies for WebSocket support and caching
channels>=4.0.0  # WebSocket support for Django
channels-redis>=4.1.0  # Redis backend for Channels
daphne>=4.0.0  # ASGI server for Channels
django-redis>=5.3.0  # Redis cache backend

# Security and rate limiting
django-ratelimit>=4.1.0  # Rate limiting for endpoints

# API
djangorestframework>=3.14.0  # REST API framework
markdown>=3.5.0  # For DRF browsable API

# Cloud Storage
django-storages[google]>=1.14.0  # Google Cloud Storage backend
google-cloud-storage>=2.10.0  # GCS Python client

# Testing
pytest>=7.4.0
pytest-django>=4.5.0
pytest-asyncio>=0.21.0
